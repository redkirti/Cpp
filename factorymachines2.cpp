#include <bits/stdc++.h>
using namespace std;
#define fast ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
#define ll long long
#define pb push_back

int main()
{
    fast
    ll n, k, j = 0;
    cin>>n>>k;
    vector <ll> time;
    for(ll i=0; i<n; i++){
        cin>>num;
        time.pb(num);
    }
    sort(time.begin(), time.end());
    
    return 0;
}